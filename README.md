# 望冉webui
后端连接至[wangran](https://github.com/CrispyXYZ/wangran)
# build：
在项目根目录：
```bash
npm install
```
在`.env`文件内填好后端ip和端口
执行
```bash
npm run build
```
来完成build，dist文件夹为生成的静态文件
# 本地预运行：
执行
```bash
npm run preview
```